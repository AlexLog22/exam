#include <stdio.h>

int main(){
    int n;
    printf("Enter number of elements: ");
    scanf("%d", &n);

    int a[n];

    printf("Wirte elements ");
    for(int i = 0; i < n; i++){
        scanf("%d", &a[i]);
    }

    int non-unque = 0;
    int unque = 0;



}